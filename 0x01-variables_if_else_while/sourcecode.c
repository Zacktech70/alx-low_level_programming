#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/**
 * main - Prints if number is positive,
 * Rturn: Always (Success)
 *
 */


int main(void)

{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	return (0);
}
